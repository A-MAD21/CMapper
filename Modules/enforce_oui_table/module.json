{
  "id": "enforce_oui_table",
  "name": "Enforce OUI Table",
  "version": "1.0",
  "description": "Rename MAC-named devices using OUI vendor and optionally set device type from vendor mapping.",
  "author": "System",
  "entry_point": "enforce_oui_table.py",
  "inputs": [
    {
      "name": "override_existing_type",
      "type": "checkbox",
      "required": false,
      "default": false,
      "label": "Override existing device type",
      "description": "When enabled, OUI device_type values replace existing device types, not only empty/unknown types."
    }
  ]
}
